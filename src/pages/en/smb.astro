---
import SmbPage from '../../components/SmbPage.astro';
import { content } from '../../i18n/ui';

const lang = 'en' as const;
const t = content[lang];
---

<SmbPage lang={lang} page={t.pages.smb} title={t.meta.smbTitle} description={t.meta.smbDescription} />
