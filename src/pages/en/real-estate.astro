---
import VerticalPage from '../../components/VerticalPage.astro';
import { content } from '../../i18n/ui';

const lang = 'en' as const;
const t = content[lang];
---

<VerticalPage
  lang={lang}
  page={t.pages.realEstate}
  title={t.meta.realEstateTitle}
  description={t.meta.realEstateDescription}
  routeKey="realEstate"
/>
